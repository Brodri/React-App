import {StyleSheet} from 'react-native';
import { whileStatement } from '@babel/types';

const styles = StyleSheet.create({
  body: {
    backgroundColor: '#e4dfdf',
  },
  imageLogo: {
    width: 400,
    height: 200,
  },
  colorButton: {
    backgroundColor: '#c61f31',
    width: 300,
    height: 50,
    justifyContent: 'center',
    alignItems: 'center',
  },
  centerContent: {
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 30,
  },
  input:{
    borderColor:'black',
    borderBottomWidth:1,
    marginBottom:8,
    marginTop:9,
    marginLeft:8,
    marginRight:8,
    color:'black',
  }
});

export default styles